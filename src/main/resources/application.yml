#数据库链接信息
spring:
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    url: jdbc:mysql://localhost:3306/shiro?serverTimezone=UTC
    username: root
    password: 123
    type: com.alibaba.druid.pool.DruidDataSource
  #设置Mybatis对应的mapper文件路径
mybatis:
  mapper-locations: classpath*:mapper/*.xml
